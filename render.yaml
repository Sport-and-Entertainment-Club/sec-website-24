services:
  - type: web
    name: sec-website
    plan: free
    branch: main
    buildCommand: npm install && npm run build
    publishPath: build/
    autoDeploy: true
    regions:
      - frankfurt
